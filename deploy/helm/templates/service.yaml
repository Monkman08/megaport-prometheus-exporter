apiVersion: v1
kind: Service
metadata:
  name: {{ include "megaport-prometheus-exporter.fullname" . }}
  labels:
    {{- include "megaport-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "megaport-prometheus-exporter.selectorLabels" . | nindent 4 }}
